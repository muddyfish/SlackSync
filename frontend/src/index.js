import React from 'react';
import ReactDOM from 'react-dom';

import Setup from './setup';
import ChannelLinker from './pages/channel_linker';

ReactDOM.render(<ChannelLinker/>, document.getElementById('root'));